:kernel_bss_start

DIM(pid_alloc, 32)

DIM(pid_procs, 32)

DIM(pid_loader, 32)

DIM(pid_init, 32)

DIM(alloc_freelist, 1)

DIM(alloc_map, 256)

DIM(initstack, 8)

DIM(loaderstack, 8)

DIM(procsstack, 8)

DIM(allocstack, 8)

:kernel_bss_end